# dsh-headless-resume: standalone headless runner with --resume support.
# Profile bundles: [dsh-base, this-package]  (no dsh-headless needed)

- insert:
    - id: code-runtime
      name: '@deepseek-ai/dsh-code-runtime-worker-thread'

    - id: headless-startup
      name: '@b0o0wen/dsh-headless-resume/lib/startup.js'

    - id: headless-runner
      name: '@b0o0wen/dsh-headless-resume'
      inject: [headlessStartup]
      config:
        task: !!js ctx.headlessStartup.task
